services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: reefradar
      POSTGRES_USER: reefradar
      POSTGRES_PASSWORD: reefradar_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: